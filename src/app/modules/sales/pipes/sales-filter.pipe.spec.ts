import { SalesFilterPipe } from './sales-filter.pipe';

describe('SalesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SalesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
